---
description: Run tests, lint, and typecheck to verify the project has no errors
---

Run, in order, and report any failures with enough detail to fix them:

1. `yarn test`
2. `yarn lint`
3. `yarn tsc --noEmit`

If all three pass, say so briefly. Do not attempt to fix unrelated pre-existing failures unless asked.
