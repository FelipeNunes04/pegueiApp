# Project rules for Claude Code

- **Never add a `Co-Authored-By` line (or any co-author attribution) to git commits.** Commit messages should only contain the summary of the change.
- **Always run the test suite (`yarn test`) after making code changes**, to check for regressions/errors, before considering a task done.
- **Always run lint (`yarn lint`) as well** after making code changes.
